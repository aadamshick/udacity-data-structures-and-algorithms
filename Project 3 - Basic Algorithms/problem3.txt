This solution leverages a quicksort algorithm to sort the numbers in O(nlog(n))
time and O(1) space complexity, since it's an in-place sorting algorithm. The
final step iterates through the sorted list and sequentially generates the
largest two numbers in O(n) time and space complexity.
