This problem runs in O(n) time, because it runs through the list once,
keeping track of the min and max values along the way.  It runs in O(1) space,
because the minimum and maximum values are tracked in two constants.
